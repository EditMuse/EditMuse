# Shopify CLI ignore file for theme app extension
# Only exclude files that should NOT be deployed
# By default, Shopify CLI includes: assets/, blocks/, locales/, snippets/

# Exclude build artifacts and temp files only
dist/
*.tmp
*.temp
.cache/
node_modules/
.DS_Store
Thumbs.db
*.log

